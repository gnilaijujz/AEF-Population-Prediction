# archive\legacy_parent_model_before_aeff_merge\GNN\AEF\transferability_results

## Purpose

旧版本代码、历史实验输出和非主流程文件，仅作参考。

## Contents

- Subdirectories: 0
- Files: 12
- Representative files: `CDA_distribution.png`, `CDA_heatmap.png`, `CDA_matrix.csv`, `CDS_distribution.png`, `CDS_heatmap.png`, `CDS_matrix.csv`, `CDT_distribution.png`, `CDT_heatmap.png`, `CDT_matrix.csv`, `entropy_weights.csv`, `full_scores.csv`, `transferability_boxplot.png`

## Code

- This directory has no direct `.py` or `.js` code files.

## Path Notes

- Local project paths are relative to the repository root.
- Generated outputs should stay under `results/` or `validation/results/`; reusable inputs should stay under `data_sources/` or `model_data/`.
