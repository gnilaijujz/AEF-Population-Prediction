# model_data/pretrained_models/mlp

## Purpose

Saved pretrained or self-trained model weights and training-curve artifacts.

## Contents

- Subdirectories: 5
- Files: 0
- Key subdirectories: `selftrain_mlp_plus_building/`, `selftrain_mlp_plus_impervious/`, `selftrain_mlp_plus_ndvi/`, `selftrain_mlp_plus_nighttime_lights/`, `selftrain_mlp_plus_poi/`

## Code

- This directory has no direct `.py` or `.js` code files.

## Path Notes

- Local project paths are relative to the repository root.
- Generated outputs should stay under `results/` or `validation/results/`; reusable inputs should stay under `data_sources/` or `model_data/`.
