# model_data/pretrained_models/mlp/selftrain_mlp_plus_building

## Purpose

Saved pretrained or self-trained model weights and training-curve artifacts.

## Contents

- Subdirectories: 0
- Files: 135
- Key files: `Albany_S_MLP_loss_curves.png`, `Albany_S_MLP_model.pt`, `Albany_S_MLP_predictions.csv`, `Albany_S_MLP_tract_pred.cpg`, `Albany_S_MLP_tract_pred.dbf`, `Albany_S_MLP_tract_pred.prj`, `Albany_S_MLP_tract_pred.shp`, `Albany_S_MLP_tract_pred.shx`, `Albany_S_MLP_training_history.csv`, `Atlanta_MLP_loss_curves.png`, `Atlanta_MLP_model.pt`, `Atlanta_MLP_predictions.csv`, ... (123 more)

## Code

- This directory has no direct `.py` or `.js` code files.

## Path Notes

- Local project paths are relative to the repository root.
- Generated outputs should stay under `results/` or `validation/results/`; reusable inputs should stay under `data_sources/` or `model_data/`.
